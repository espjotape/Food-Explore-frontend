import styled from "styled-components"

export const Container = styled.section`

 > h2{
  font-weight: 600;
  font-size: 18px;
  color: ${({theme}) => theme.COLORSD.WHITE_300};
  margin-bottom: 16px;
 }
`